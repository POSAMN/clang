//определить структуру матрица 3x3
//определить операции сложения матриц и умножения матриц

//определить структуру матрица 3x3
//определить операции сложения матриц и умножения матриц

#include <stgio.h>
#include <stdlib.h>

typedef struct tre
{
    double** mas = (double**)malloc(3*sizeof(double*));
    for (int i = 0; i < 3; i++){
        mass[i] = (double*)malloc(sizeof(3*(double))); 
} treXtre_t;

int main()
{
    
}
